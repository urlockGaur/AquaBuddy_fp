plugins {
    id "com.android.application" version "8.2.1" // Update to 8.2.1
    id "kotlin-android"         // Update to the latest compatible Kotlin version
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.finalproject.aquabuddy"
    compileSdk = 34
    ndkVersion = "25.1.8937393" // Use the required version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.finalproject.aquabuddy"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }

    java {
        toolchain {
            languageVersion.set(JavaLanguageVersion.of(17))
        }
    }
}

flutter {
    source = "../.."
}
